<% if artist.favorited_by?(current_customer) %>
  <%= link_to front_artist_favorite_artists_path(artist), method: :delete, remote: true do %>
    ❤︎<%= artist.favorite_artists.count %>
  <% end %>
  <% else %>
    <%= link_to front_artist_favorite_artists_path(artist), method: :post, remote: true do %>
      ♡<%= artist.favorite_artists.count %>
  <% end %>
<% end %>

<% if flash.any? %>
  <script type="text/javascript">
    <% flash.each do |key, value| %>
      <% key = "success" if key == "notice" %>
      <% key = "error" if key == "alert" %>
      toastr['<%= key %>']('<%= value %>');
    <% end %>
  </script>
<% end %>