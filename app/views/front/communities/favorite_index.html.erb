<div align="center">
  <div class="content_border">
  	<h4 class="favorite_index_headline">お気に入りアーティストのコミュニティ一覧</h3>
  		
      <% current_customer.favorite_artist_artists.each do |artist| %>
      <div class="flex">
      <div class="favorite_index_box">
        <%= image_tag artist.image_id.to_s, :size => '100x100' %>
        <%= link_to artist.name, front_community_path(artist), class: "btn_favorite_name" %>
      </div>
      </div>
      <% end %>
  </div>
</div>